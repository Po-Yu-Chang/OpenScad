# OpenCad CAD Worker — Python 依賴
# 安裝方式：pip install -r requirements.txt
# 注意：build123d 需要 OCP (OpenCascade Python) wheel，三平台皆有。

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
build123d>=0.7.0
pydantic>=2.0.0
jsonschema>=4.20.0
trimesh>=4.0.0
numpy>=1.24.0

# 開發用
# pytest>=7.0.0
httpx>=0.25.0  # tests/prompts/gateway_client.py 真 gateway 端到端測試用（WP-H1）